apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-frontend
  labels:
    app: social-frontend
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: social-frontend
    spec:
      containers:
      - name: social-frontend
        image: soberservicesguy/portfolio-images:socialapp_frontend_25
        ports:
          - containerPort: 80
            name: http-port
        resources:
          limits:
            cpu: "50m"
            memory: "150Mi"
        env:
        - name: TODO_SERVER
          value: todo-backend-service
      imagePullSecrets:
      - name: regcred
  selector:
    matchLabels:
      app: social-frontend
---
apiVersion: v1
kind: Service
metadata:
  name: social-frontend-service
spec:
  type: NodePort
  selector:
    app: social-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80